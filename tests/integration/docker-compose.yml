name: fume-mapping-provider_hapi_tests

services:
  hapi-fhir:
    image: hapiproject/hapi:latest
    container_name: fume-mapping-provider-test-hapi
    ports:
      - "8081:8080"
    environment:
      - hapi.fhir.fhir_version=R4
      - hapi.fhir.server_address=http://localhost:8081/fhir
    volumes:
      - ./hapi.application.yaml:/app/config/application.yaml:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/fhir/metadata"]
      interval: 10s
      timeout: 5s
      retries: 30
